import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const investorImporterSelectors = importerSelectors(
  'investor.importer',
);

export default investorImporterSelectors;
