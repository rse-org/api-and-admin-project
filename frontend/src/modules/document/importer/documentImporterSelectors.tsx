import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const documentImporterSelectors = importerSelectors(
  'document.importer',
);

export default documentImporterSelectors;
